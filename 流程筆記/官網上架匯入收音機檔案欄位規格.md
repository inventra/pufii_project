# {日期}官網上架匯入收音機的檔案 欄位規格

## 檔案/資料夾
- 資料夾名稱：`{日期}官網上架匯入收音機的檔案`。
- 範例：`0414官網上架匯入收音機的檔案`。
- 目前資料夾：`/Users/openclaw/創旭/帕妃/輸出檔/0414官網上架匯入收音機的檔案`。

## 欄位清單

| 順序 | 欄位名稱 |
|---:|---|
| 1 | 第一碼(特價碼) |
| 2 | 物品編號 |
| 3 | 物品名稱 |
| 4 | 簡介 |
| 5 | 小圖片路徑(180x180) |
| 6 | 原價 |
| 7 | 價格 |
| 8 | 物品排序 |
| 9 | 重量(g) |
| 10 | ps |
| 11 | 銷售 |
| 12 | 尺寸表連結 |
| 13 | 試穿報告連結 |
| 14 | 手機版原始碼 |
| 15 | 原價標 |

## 欄位來源/固定值

| 欄位 | 規則 |
|---|---|
| 第一碼(特價碼) | 固定 `Z` |
| 物品編號 | `商品資料大檔.xlsx` → `流水號` 分頁 → 對應流水號 A 欄 `流水號` |
| 物品名稱 | `商品資料大檔.xlsx` → `流水號` 分頁 → 對應流水號 D 欄 `名稱` |
| 簡介 | 固定空白 |
| 小圖片路徑(180x180) | 暫定空白 |
| 原價 | `商品資料大檔.xlsx` → `商品資料(大檔)` → 篩選列 S 欄 `原價` |
| 價格 | `商品資料大檔.xlsx` → `商品資料(大檔)` → 篩選列 T 欄 `新品價` |
| 物品排序 | 固定 `0` |
| 重量(g) | `商品資料大檔.xlsx` → `流水號` 分頁 → 對應流水號 E 欄 `重量` + 30 |
| ps | 固定空白 |
| 銷售 | 固定 `1` |
| 尺寸表連結 | 固定空白 |
| 試穿報告連結 | 固定空白 |
| 手機版原始碼 | 固定空白 |
| 原價標 | 固定 `1` |

## 已確認共通規則
- 檔案格式：全部輸出為 `.xlsx`。
- 篩選商品條件：`商品資料(大檔)` A 欄 `記號={日期}` 且 B 欄 `用途=上架`（直播日期 B 欄刪除後，原用途欄前移為 B 欄）。


## 2026-05-09 程式草稿與試跑
- 程式：`/Users/openclaw/創旭/帕妃/程式/generate_website_radio.py`
- 已試跑：`--date 0414`
- 輸出資料夾：`/Users/openclaw/創旭/帕妃/輸出檔/0414官網上架匯入收音機的檔案`
- 輸出檔命名：`{日期}新品-檔案2-名稱+大小.xlsx`。
- 範例輸出檔：`/Users/openclaw/創旭/帕妃/輸出檔/0414官網上架匯入收音機的檔案/0414新品-檔案2-名稱+大小.xlsx`
- audit：同檔名後加 `.audit.json`
- 結果：篩選 `商品資料(大檔)` A 欄 `記號={日期}` 且 B 欄 `用途=上架`。
- 欄位確認：共 15 欄，最後一欄 O `原價標`。

## 2026-05-09 新增：檔案4 上架分類
- 程式：`/Users/openclaw/創旭/帕妃/程式/generate_website_category_file4.py`
- 輸出檔命名：`{日期}新品-檔案4-上架分類-2可以上架.xlsx`。
- 範例輸出檔：`/Users/openclaw/創旭/帕妃/輸出檔/0414官網上架匯入收音機的檔案/0414新品-檔案4-上架分類-2可以上架.xlsx`
- 篩選：`商品資料(大檔)` A 欄 `記號={日期}` 且 B 欄 `用途=上架`。
- 欄位：A `物品編號` ← E 欄 `流水號`；B `分類編號` 固定展開兩列：`1701`、`1535`（每個流水號都出現兩列）。
- 0414 試跑結果：1 個流水號，輸出 2 筆：`P33034 / 1701`、`P33034 / 1535`。
- audit：同檔名後加 `.audit.json`。

## 2026-05-09 新增：檔案4 隱藏分類
- 程式：`/Users/openclaw/創旭/帕妃/程式/generate_website_hidden_category_file4.py`
- 輸出檔命名：`{日期}新品-檔案4-隱藏分類-1給客服檢查.xlsx`。
- 範例輸出檔：`/Users/openclaw/創旭/帕妃/輸出檔/0414官網上架匯入收音機的檔案/0414新品-檔案4-隱藏分類-1給客服檢查.xlsx`
- 篩選：`商品資料(大檔)` A 欄 `記號={日期}` 且 B 欄 `用途=上架`。
- 欄位：A `物品編號` ← E 欄 `流水號`；B `分類編號` 固定 `1370`。
- 0414 試跑結果：1 筆，`P33034 / 1370`。
- audit：同檔名後加 `.audit.json`。

## 2026-05-09 新增：檔案7 洗滌+通用+標籤
- 程式：`/Users/openclaw/創旭/帕妃/程式/generate_website_tags_file7.py`
- ext03 內容模板：`/Users/openclaw/創旭/帕妃/程式/templates/file7_ext03_washing.html`
- 輸出檔命名：`{日期}新品-檔案7-洗滌+通用+標籤-2.xlsx`。
- 範例輸出檔：`/Users/openclaw/創旭/帕妃/輸出檔/0414官網上架匯入收音機的檔案/0414新品-檔案7-洗滌+通用+標籤-2.xlsx`
- 篩選：`商品資料(大檔)` A 欄 `記號={日期}` 且 B 欄 `用途=上架`。
- 欄位：A `物品編號` ← E 欄 `流水號`；B `群組`；C `內容`。
- 每個流水號固定展開 3 列：`ext03`（洗滌 HTML）、`label`（限時SALE 標籤）、`listcustomicon`（優惠/春裝新品SALE/會員紅利 HTML）。
- 0414 試跑結果：1 個流水號，輸出 3 筆：`P33034 / ext03`、`P33034 / label`、`P33034 / listcustomicon`。
- audit：同檔名後加 `.audit.json`。

## 待整併
- 目前各輸出檔先拆成獨立程式方便確認規則；最後需整併成一支主程式，一次產出所有官網上架匯入收音機相關檔案。
- 需納入/對齊既有 91 檔流程與 audit（使用者補充：最後要一起合併）。

## 2026-05-09 反推：檔案3 色塊
- 參考檔：Google Drive `0414新品-檔案3-色塊.xls`，已確認可由 `0414-電子房名(刊登).xlsx` 的 `色塊>>待編輯` 分頁完整還原；32 筆資料列逐列一致。
- 輸出檔命名：`{日期}新品-檔案3-色塊.xlsx`（使用者已確認輸出皆為 xlsx；範例正確檔雖為 xls，正式先按 xlsx）。
- 欄位：`物品編號`, `顏色`, `色塊路徑`, `瀏覽用小圖`, `大圖路徑`, `超大圖路徑`, `排序`, `銷售`。
- 新版來源邏輯：`商品資料(大檔).xlsx` 內直接包含 `大師` 分頁，不再依賴電子房名檔。
- 檔名：`{日期}新品-檔案3-色塊.xlsx`。
- 篩選流水號：`商品資料(大檔)` 分頁 A 欄 `記號 = 日期` 且 B 欄 `用途 = 上架`，取 E 欄 `流水號`。
- 每個篩選出的流水號，到 `大師` 分頁 A 欄 `物品編號` 找所有商店顏色；以「同一流水號 + 商店顏色」去重，不因尺寸不同重複產生色塊。
- 欄位規則：
  - `物品編號`：篩選出的當日流水號 / `大師!A 物品編號`
  - `顏色`：`大師!J 商店顏色`
  - `色塊路徑`：`https://photo.pufii.com.tw/{year}/{日期}-C/{大師!AC 圖片路徑}-{大師!AD 色塊號碼}.jpg`
  - `瀏覽用小圖`：固定空白
  - `大圖路徑`：固定空白
  - `超大圖路徑`：固定空白
  - `排序`：`大師!AD 色塊號碼 - 2`
  - `銷售`：固定 `1`
- `色塊號碼`、`中圖張數` 以「同一流水號 + 商店顏色」為單位填寫，不因尺寸重複產生色塊/中圖。記錄檔：`/Users/openclaw/創旭/帕妃/流程筆記/給顧客確認_大檔需補欄位.md`。
- Reset 規則：若使用者重置初始檔並刪除目前範例/正確檔，後續只以新的初始大檔與尺寸表重新推導，不依賴已刪除範例檔或硬寫目前正確答案。

## 2026-05-09 新增：檔案5 中圖合圖
- 程式：`/Users/openclaw/創旭/帕妃/程式/generate_website_middle_images_file5.py`
- 輸出檔命名：`{日期}新品-檔案5-中圖合圖.xlsx`。
- 分頁名稱：`檔案5.購物網站顏色-多小圖匯入-匯入格式`。
- 篩選流水號：`商品資料(大檔)` 分頁 A 欄 `記號 = 日期` 且 B 欄 `用途 = 上架`，取 E 欄 `流水號`。
- 每個篩選出的流水號，到 `大師` 分頁 A 欄 `物品編號` 找所有商店顏色；以「同一流水號 + 商店顏色」去重，不因尺寸不同重複產生中圖。
- 欄位規則：
  - `物品編號`：篩選出的當日流水號 / `大師!A 物品編號`
  - `顏色`：`大師!J 商店顏色`
  - `物品明細小圖路徑`：固定空白
  - `物品明細中圖路徑`：依 `大師!AE 中圖張數` 展開 `https://photo.pufii.com.tw/{year}/{日期}/{大師!AC 圖片路徑}-{大師!AD 色塊號碼}_01.jpg` 到 `_NN.jpg`
  - `物品明細大圖路徑`：固定空白
  - `購物上架`：固定 `1`
  - `排序`：中圖序號 `1..中圖張數`
  - `款式顏色`：`物品編號-顏色`
  - `中途路徑-前半`：`https://photo.pufii.com.tw/{year}/{日期}/{大師!AC 圖片路徑}-{大師!AD 色塊號碼}`
- 0414 測試輸出：`/Users/openclaw/創旭/帕妃/輸出檔/0414官網上架匯入收音機的檔案/0414新品-檔案5-中圖合圖.xlsx`，資料列 58 筆、缺漏 0。

## 2026-05-09 新增：推薦待填寫
- 程式：`/Users/openclaw/創旭/帕妃/程式/generate_website_recommend_pending.py`
- 輸出檔命名：`{日期}新品-推薦(待填寫).xlsx`。
- 範例輸出檔：`/Users/openclaw/創旭/帕妃/輸出檔/0414官網上架匯入收音機的檔案/0414新品-推薦(待填寫).xlsx`
- 篩選：`商品資料(大檔)` A 欄 `記號={日期}` 且 B 欄 `用途=上架`。
- 欄位：A `主物品編號` ← E 欄 `流水號`；B `推薦物品編號` 固定空白；C `排序` 固定空白。
- 0414 試跑結果：1 筆，`P33034`。
- audit：同檔名後加 `.audit.json`。
## 2026-05-09 欄位位移確認
- 使用者確認所有輸出都是 `.xlsx`。
- `商品資料(大檔)` 已刪除原 B 欄 `直播日期`，因此原 C 欄 `用途` 前移為 B 欄。
- 後續 `商品資料(大檔)` 欄位也前移一欄：例如 `流水號` 由 F→E、91 檔使用的 `完整房名` AA→Z、`原價` T→S、`新品價` U→T、`ray成本` L→K。
- 日期預設：正式執行時用點擊/執行程式當下日期 `MMDD`；前期測試可用參數指定，例如 `--date 0414`。

