# 帕妃直播表格自動化｜問題包（待問老闆）

> 目的：人工流程轉自動化前，集中確認規則，避免一邊做一邊猜。

## A. 日期與篩選規則

1. 正式抓當日資料時，要篩哪一欄？
   - 目前測試：`商品資料(大檔)` → `記號 = 0414`
   - 但大檔也有 `直播日期` 欄，未來是否應該改篩 `直播日期`？

2. 日期格式要統一成哪種？
   - `0414`
   - `4/14`
   - Excel 日期
   - 其他

3. 每週二、四跑流程時，日期是否永遠用執行當天？
   - 如果補跑過去日期，是否允許手動指定？

## B. 直播表格主欄位規則

4. `備註` 欄是否永遠空白？
   - 還是有些情況要帶「搭配」或其他文字？

5. `標數` 欄目前確認先空白，是否固定都空白？
   - 還是未來需要自動產生 `第01標`、`第02標`？

6. `{日期}直播` 欄目前改用 `房名`，是否固定用大檔的 `房名`？
   - 不用 `形容`
   - 不用 `完整房名`

7. `廠商` 欄要帶哪個來源？
   - `國內廠商代號`
   - `國外廠商代號`
   - `廠商+貨號`
   - 還是先空白？

## C. 價格與成本規則

8. `原價` 是否固定抓大檔 `原價`？

9. `直播價` 是否固定抓大檔 `直播+1價`？
   - 如果 `直播+1價` 空白，要不要改抓 `新品價`？
   - 還是應該留空？

10. `成本` 欄要抓哪個？
   - 目前試作：優先抓 `ray成本`
   - 若空白再抓 `國內成本`
   - 是否正確？

11. 直播主表 `0414` 的成本是否應固定顯示 500？
   - 因為範例舊檔主表成本看起來有 500 的情況
   - 但 `RAY` 分頁成本看起來像真實成本
   - 需確認主表與 RAY 成本規則是否不同

## D. 尺寸表規則

12. `推薦尺寸` 是否固定抓尺寸檔的 `適合尺寸`？
   - 例如 `S-XL` → 輸出 `S-XL適合`
   - 如果空白，要不要留空？

13. `尺寸表` 欄目前規則是否正確？
   - 以流水號去尺寸檔各分頁找
   - 組合欄位：領口、肩寬、袖長(領-袖)、袖寬、胸寬、腰寬、臀寬、下擺、全長
   - 空白欄位不輸出
   - `下擺` 輸出成 `下擺寬`

14. 如果同一個流水號在尺寸表有多列，例如 S/M/L/XL，要怎麼輸出？
   - 全部尺寸都合併？
   - 每個尺寸換行？
   - 只抓第一列？

15. 不同類型尺寸表欄位不同時，是否都照該分頁有值的欄位組合？
   - 上衣/洋裝
   - 褲/裙
   - 配件
   - 鞋
   - 內衣/泳裝

## E. 顏色規則

16. `顏色` 欄要從哪裡來？
   - 尺寸檔的 `顏色尺寸`
   - 大檔的 `顏色尺寸請用這一個`
   - 其他分頁
   - 還是人工填？

17. 如果尺寸檔 `顏色尺寸` 是尺寸代號，例如 F、S、M、L，不是真正顏色，是否不能拿來當顏色？

18. 顏色輸出格式要怎麼寫？
   - `黑、白、灰`
   - `黑S-XL、白S-XL`
   - 依客戶舊表格式

## F. 分頁衍生規則

19. `對款` 分頁是否永遠從主表取以下欄位？
   - 備註、貨號、標數、{日期}直播、推薦尺寸、顏色

20. `姐` 分頁是否永遠從主表取以下欄位？
   - 標數、{日期}直播、推薦尺寸、顏色

21. `RAY` 分頁是否永遠從主表取以下欄位？
   - 備註、貨號、標數、{日期}直播、推薦尺寸、顏色、原價、直播價、成本、新品價、廠商

22. 分頁順序是否固定為：
   - `{日期}`
   - `對款`
   - `姐`
   - `RAY`

## G. 檔案與路徑規則

23. 客戶電腦上來源資料夾是否固定？
   - 商品大檔放哪裡？
   - 尺寸表&試穿報告放哪裡？

24. 輸出檔是否固定放在同一個輸出資料夾？

25. 輸出檔名是否固定：`{日期}直播表格.xlsx`？

26. 如果輸出檔已存在，要覆蓋還是另存新版？

## H. 第一版範圍確認

27. 第一版是否只做直播表格？
   - 先不做 91
   - 先不做官網
   - 先不做 VIP / ERP

28. 第一版是否接受以下欄位人工補？
   - 備註
   - 標數
   - 顏色
   - 廠商
   - 圖片/色塊相關欄位

29. 第一版完成標準是什麼？
   - 可產出四個分頁
   - 筆數正確
   - 價格/成本正確
   - 尺寸表文字正確
   - 其他欄位可人工補
