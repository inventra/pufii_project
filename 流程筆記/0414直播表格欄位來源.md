# 0414直播表格｜欄位來源試算 v1

## 輸入/輸出
- 輸入檔：`/Users/openclaw/創旭/帕妃/輸入檔/商品資料大檔.xlsx`
- 來源分頁：`商品資料(大檔)`
- 篩選條件：`記號 = 0414`
- 輸出檔：`/Users/openclaw/創旭/帕妃/輸出檔/0414直播表格.xlsx`
- 本次筆數：10 筆

## 重要前提
- 程式未來要搬到客戶電腦，所以來源資料夾與日期不可寫死；本次先假設來源路徑為 `/Users/openclaw/創旭/帕妃/輸入檔/商品資料大檔.xlsx`。
- 本版先只使用 `商品資料(大檔)` 可直接取得的欄位。
- `標數` 依使用者確認需空白。
- `0414直播` 依使用者確認使用 `房名` 欄。
- `推薦尺寸`、`顏色`、`尺寸表` 目前大檔此分頁無法直接確認來源，先留空。

## 欄位來源

| 輸出欄位 | 來源檔 | 來源分頁 | 來源欄位 | 本版規則 |
|---|---|---|---|---|
| 備註 | 無 | 無 | 無 | 先留空，疑似人工欄位 |
| 貨號 | 商品資料大檔.xlsx | 商品資料(大檔) | 流水號（第6欄） | 直接帶出，例如 P33034 |
| 標數 | 無 | 無 | 無 | 空白，給客戶後續填寫 |
| 0414直播 | 商品資料大檔.xlsx | 商品資料(大檔) | 房名（第18欄） | 直接帶 `房名` |
| 推薦尺寸 | 待確認 | 待確認 | 待確認 | 本版先留空 |
| 顏色 | 待確認 | 待確認 | 待確認 | 本版先留空 |
| 原價 | 商品資料大檔.xlsx | ray價錢 | L 欄 `原價` | 以流水號對應 ray價錢 I 欄後取 L 欄 |
| 直播價 | 商品資料大檔.xlsx | ray價錢 | O 欄 `直播+1價` | 以流水號對應 ray價錢 I 欄後取 O 欄 |
| 尺寸表 | 待確認 | 待確認 | 待確認 | 本版先留空 |
| 成本 | 商品資料大檔.xlsx | 商品資料(大檔) | ray成本（第12欄） | 優先帶 `ray成本`；若空再用國內成本 |
| 新品價 | 商品資料大檔.xlsx | 商品資料(大檔) | 新品價（第21欄） | 直接帶出 |
| 廠商 | 商品資料大檔.xlsx | 商品資料(大檔) | 國內廠商代號（第9欄） | 直接帶出；若空暫留空 |

## 分頁生成規則

### 0414
欄位：備註、貨號、標數、0414直播、推薦尺寸、顏色、原價、直播價、尺寸表、成本、新品價、廠商

### 對款
從 `0414` 分頁取：備註、貨號、標數、0414直播、推薦尺寸、顏色

### 姐
從 `0414` 分頁取：標數、0414直播、推薦尺寸、顏色

### RAY
從 `0414` 分頁取：備註、貨號、標數、0414直播、推薦尺寸、顏色、原價、直播價、成本、新品價、廠商

## 待確認
1. 正式篩選欄位是 `記號` 還是 `直播日期`？
2. `0414直播` 應該用 `形容`、`房名` 還是 `完整房名`？
3. `成本` 主表是否應固定 500，還是使用大檔 `ray成本`？
4. `推薦尺寸`、`顏色`、`尺寸表` 的正式來源分頁/欄位。
5. `廠商` 是否要用國內廠商代號、國外廠商代號，或另一個對照表。


## 2026-05-08 使用者修正
- `標數` 不自動產生，改為空白。
- `0414直播` 欄位來源改用 `商品資料(大檔)` 的 `房名` 欄。
- 已重新輸出 `輸出檔/0414直播表格.xlsx`。


## 日期規則補充
- 正式流程不是固定 `0414`。
- 流程每週二、四執行，日期應跟著當天日期自動判斷。
- `0414` 只是目前用來測試與回溯的範例日期。
- 未來程式應提供可調整參數：
  - 預設：用執行當天日期轉成 `MMDD`，例如 4/14 → `0414`。
  - 可手動指定日期：方便補跑或測試，例如指定 `0414`。
- 輸出檔名稱應依日期產生：`{日期}直播表格.xlsx`，例如 `0414直播表格.xlsx`。
- 分頁與欄位名稱中的日期也應依日期替換，例如 `0414` 分頁、`0414直播` 欄。


## 尺寸表欄位規則補充

### 尺寸表來源
- 來源檔：`輸入檔/尺寸表&試穿報告.xlsx`
- 以直播表的 `貨號` / 大檔 `流水號` 對應尺寸表各分頁的 `流水號` 欄。
- 例如：`P33034` 在 `上衣or洋裝or吊帶裙` 分頁第 965 列。

### 尺寸表輸出組合規則
目前確認需要把尺寸欄位組成一段文字後填入直播表 `尺寸表` 欄。

以 `P33034` 為例，來源欄位：
- 領口
- 肩寬
- 袖長(領-袖)
- 袖寬
- 胸寬
- 腰寬
- 臀寬
- 下擺
- 全長

實際資料：
- 肩寬 = 34
- 袖長 = 16
- 袖寬 = 18
- 胸寬 = 31
- 下擺 = 74
- 全長 = 130

輸出格式：
`肩寬34 袖長16 袖寬18 胸寬31 下擺寬74 全長130`

### 空值規則
- 空白欄位不要輸出。
- `下擺` 輸出名稱改成 `下擺寬`。
- `袖長(領-袖)` 輸出名稱改成 `袖長`。


## 尺寸表修正：兩件式需用 `//` 分隔

### 問題原因
- `上衣+小可愛(兩件式)` 分頁有兩組尺寸欄位。
- 之前因為分頁內有重複欄名，例如兩個 `全長`，程式只用欄名查找時會抓到後面那組，導致 P33315 組成：`肩寬39 袖長13 袖寬17 胸寬40 全長49`。
- 正確做法是兩組尺寸分開組合，中間用 `//`。

### P33315 正確尺寸表
來源：`尺寸表&試穿報告.xlsx` → `上衣+小可愛(兩件式)` 第 37 列。

輸出：
`肩寬39 袖長13 袖寬17 胸寬40 全長54 // 袖寬26 胸寬45 全長49`

### 規則
- 第一組：上衣尺寸。
- 第二組：小可愛/下身/第二件尺寸。
- 兩組中間使用 ` // ` 區隔。
- 空白欄位不輸出。


## 顏色欄位規則補充

### 來源分頁
顏色不是直接從尺寸表抓，而是由員工先在大檔內填入：

1. `本次要匯入的流水號`
   - A 欄：`要匯入的流水號`
   - B 欄：`顏色選項個數`

2. `顏色尺寸請用這一個`
   - A 欄：`物品編號`
   - H 欄：`顏色`

### 顏色產生規則
以每個直播商品流水號為 key：

1. 先到 `本次要匯入的流水號` 找該流水號。
2. 取得該流水號的 `顏色選項個數`，例如 3。
3. 再到 `顏色尺寸請用這一個` 找相同 `物品編號` 的所有列。
4. 做第二層防護：
   - `顏色尺寸請用這一個` 中該流水號的列數，必須等於 `本次要匯入的流水號` 的 `顏色選項個數`。
   - 若數量不一致，應標記異常，不要安靜帶錯。
5. 若數量一致，取出該流水號所有列的 H 欄 `顏色`。
6. 用 `、` 串接後填入直播表 `顏色` 欄。

### 範例
如果：
- `本次要匯入的流水號`：P33344 顏色選項個數 = 3
- `顏色尺寸請用這一個`：P33344 有 3 列，H 欄為 `白F`、`深灰F`、`黑F`

則直播表 `顏色` 輸出：
`白F、深灰F、黑F`

### 注意
- 目前 0414 測試資料中的 P33034、P33315 在 `本次要匯入的流水號` 與 `顏色尺寸請用這一個` 找不到，代表這份大檔目前的顏色分頁資料不是 0414 那批，或尚未由員工填入。
- 未來正式流程需先確認員工已完成 `本次要匯入的流水號` 填寫，才可產生顏色。


## 顏色欄位目前決議與格式規則

### 目前決議
- 直播表 `顏色` 欄目前先空白，不直接從 `流水號` 分頁 H 欄補。
- 顏色來源差異已列入待確認問題包。

### 未來若抓到顏色，輸出格式
- 多個顏色之間使用 `、` 分隔。
- 如果來源是 `{顏色}{尺寸}` 或 `{顏色}-{尺寸}` 形式，需要先依顏色整合尺寸。
- `F` 代表 free size，不特別再標一次尺寸。

### 整合範例
來源：
```text
淺藍S
淺藍M
藍S
藍M
藍L
藍XL
```

輸出應整合為：
```text
淺藍S-M、藍S-XL
```

如果尺寸中間跳號，例如只有 S、L：
```text
黑S/L
```

如果是 F：
```text
白
```
不要輸出成 `白F`。


## 價格欄位來源修正與驗證

### 使用者確認
- `原價` 來源：`商品資料大檔.xlsx` → `ray價錢` 分頁 L 欄。
- `直播價` 來源：`商品資料大檔.xlsx` → `ray價錢` 分頁 O 欄。

### 對應方式
- 用直播商品的 `流水號` 對應 `ray價錢` 分頁 I 欄 `流水號`。
- 找到後取：
  - L 欄：`原價`
  - O 欄：`直播+1價`

### 0414 驗證結果
已用 0414 的 10 筆驗證：
- `商品資料(大檔)` 原價欄 = `ray價錢` L 欄。
- `商品資料(大檔)` 直播+1價欄 = `ray價錢` O 欄。
- 10/10 筆一致。

因此後續自動化應以 `ray價錢` L/O 作為價格正式來源。


## 成本欄位來源待確認

### 使用者補充
- 成本欄位目前先記錄來源可能在 `ray價錢` 分頁 K 欄與 M 欄。
- 但目前抓到的成本與使用者知道的正確答案不一致。

### 決議
- 成本不可直接定案。
- 需向帕妃/客戶確認：
  1. 主直播表的 `成本` 欄到底要抓 `ray價錢` 哪一欄。
  2. 若與檔案內成本不同，是否有人工調整、固定加成、或另一份來源表。
  3. RAY 分頁與主表成本是否使用同一套成本邏輯。


## 檔案命名與迭代規則
- 使用者確認：檔案名稱不要任意修改。
- 除非使用者明確指定要區分版本，否則同一份輸出都用原檔名迭代覆蓋/更新。
- 例如 0414 測試直播表固定使用：`0414直播表格.xlsx`。


## 推薦尺寸填入規則
- 使用者確認：`推薦尺寸` 要跟 `尺寸表` 一起查、一起填入。
- 來源同樣是 `尺寸表&試穿報告.xlsx`，用商品 `流水號` 搜尋全部尺寸分頁。
- 找到該流水號後：
  - `推薦尺寸` 取該列 `適合尺寸` 欄，輸出格式為 `{適合尺寸}適合`。
  - `尺寸表` 同列組合肩寬、袖長、胸寬、全長等欄位。
- 若該列沒有 `適合尺寸`，才保留空白並列入檢查，不可只因主表空白就放棄查尺寸分頁。
- 0414 目前 10 筆中有 7 筆可帶入推薦尺寸；P33296、P33315、P33317 尺寸表有找到，但 `適合尺寸` 欄本身空白。


## 標數與顏色修正確認
- 使用者確認：`標數` 仍然要空白；正確檔內的標數先不作為自動化輸出來源。
- `顏色` 不能再直接忽略，需要回頭看來源與補齊規則。
- 後續顏色要優先比對正確檔顏色、`流水號` 分頁 H 欄、以及 `本次要匯入的流水號` / `顏色尺寸請用這一個` 的產生結果。


## 顏色來源待確認
- 使用者確認：顏色目前不能直接定案，因為來源對不上。
- 雖然部分商品可從 `流水號` 分頁 H 欄對到正確檔，但仍有例外：
  - P33061：正確檔 `白、卡`，但 `流水號` H 欄空白。
  - P33317：`流水號` H 欄混入備註，需要人工判斷清理。
- 因此顏色欄需先向帕妃/客戶確認正式來源與清理規則，再決定是否自動補。
- 在確認前，自動化不可把 `流水號` H 欄視為正式唯一來源。


## 0414 比對後決議更新
- 需問客戶：顏色來源、主表成本邏輯、推薦尺寸來源、P33315 品名差異。
- 不需處理：主表第 13 欄備註；使用者確認這個不需要產出。
- 已確認要修：P33115 下身尺寸欄位抓錯，應抓第二組 `腰寬 鬆緊帶x~x=30-41 / 臀寬=46 / 大腿=28 / 褲檔=33 / 全長=34`。
- 已確認要修：P33282 肩帶欄位輸出為 `肩帶17-29`，不要變成 `肩寬肩帶(17-29)`。
- 已確認要修：多件式 `//` 後不加空格。


## 已寫入 0414 輸出檔的正確欄位
已更新 `/Users/openclaw/創旭/帕妃/輸出檔/0414直播表格.xlsx`，不改檔名，直接迭代覆蓋。

本次只寫入已確認的尺寸表格式修正：
- P33115：`領口寬17 肩寬47 袖長61 袖寬27 胸寬63 全長66 // 腰寬30-41 臀寬46 大腿28 褲檔33 全長34`
- P33282：`肩帶17-29 袖寬23 胸寬44 下擺寬103 全長124`
- P33315：`肩寬39 袖長13 袖寬17 胸寬40 全長54 //袖寬26 胸寬45 全長49`

仍未寫入，需先確認：顏色、主表成本、推薦尺寸、P33315 品名差異。
標數已維持空白。


## 尺寸表注記：上衣+下身需讀 Q-W
- 使用者補充：若商品在 `尺寸表&試穿報告.xlsx` 的 `上衣+下身` 分頁找到，除了前段上衣尺寸外，下身尺寸要多看 Q-W 欄。
- Q-W 欄對應：
  - Q：`腰寬 鬆緊帶x~x`
  - R：`臀寬`
  - S：`大腿`
  - T：`褲檔`
  - U：`下擺`
  - V：`全長`
  - W：`褲管`
- 輸出規則：空白欄位不輸出；`大腿` 先照原欄名輸出為 `大腿`，不要自行改成 `大腿寬`；上下身中間用 `//` 分隔。
- P33115 範例：Q-W 為 `30-41 / 46 / 28 / 33 / 空白 / 34 / 空白`，輸出：`// 腰寬30-41 臀寬46 大腿28 褲檔33 全長34`。


## 大腿欄位命名修正
- 使用者指出：尺寸表欄位名稱是 `大腿`，不應自行改成 `大腿寬`。
- 後續輸出照原欄名：`大腿28`。
- 除非客戶明確要求，才改成 `大腿寬`。


## 尺寸欄位名稱保留規則
- 使用者補充：尺寸表可能同時出現 `下擺`、`下擺寬`、`大腿`、`大腿寬`。
- 規則：輸出時以來源欄位實際名稱為準，不自行互改。
  - 來源是 `下擺` → 輸出 `下擺`。
  - 來源是 `下擺寬` → 輸出 `下擺寬`。
  - 來源是 `大腿` → 輸出 `大腿`。
  - 來源是 `大腿寬` → 輸出 `大腿寬`。
- 例外：只有已確認的特殊欄位才做清理，例如 P33282 的 `肩帶(17-29)` 應輸出 `肩帶17-29`。
