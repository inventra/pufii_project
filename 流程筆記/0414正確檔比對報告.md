# 0414 正確檔比對報告

## 比對範圍
- 我們的輸出檔：`/Users/openclaw/創旭/帕妃/輸出檔/0414直播表格.xlsx`
- 正確檔：`/Users/openclaw/創旭/帕妃/輸入檔/0414正確檔.xlsx`
- 只比對我們手上 10 個流水號：P33034、P33061、P33103、P33104、P33115、P33232、P33282、P33296、P33315、P33317
- 正確檔多出來的 25 個商品編號先忽略。

## 分頁比對結果

### 0414 主表
共同差異：
- 正確檔有 `標數`，我們目前空白。
- 正確檔有 `顏色`，我們目前空白。
- 正確檔第 13 欄有備註，例如：`不拍不上 有現貨要排現貨場`、`4/15沒開`，我們目前沒有此欄。
- 主表 `成本`：正確檔 10 筆都是 `500`，我們目前抓檔案成本，與正確檔不同。

個別差異：
- P33115 尺寸表不同：
  - 我們：`領口17 肩寬47 袖長61 袖寬27 胸寬63 全長66 // 腰寬46 臀寬28 大腿33 下擺寬34`
  - 正確：`領口寬17 肩寬47 袖長61 袖寬27 胸寬63 全長66 // 腰寬30-41 臀寬46 大腿寬28 褲檔33 全長34`
- P33282 尺寸表文字不同：
  - 我們：`肩寬肩帶(17-29) 袖寬23 胸寬44 下擺寬103 全長124`
  - 正確：`肩帶17-29 袖寬23 胸寬44 下擺寬103 全長124`
- P33296 推薦尺寸：正確檔 `S-XL適合`，我們空白。
- P33315：
  - 主表品名文字不同：正確為 `圓領素面短袖T恤上衣+針織細肩帶背心兩件式套裝`
  - 推薦尺寸正確檔 `S-XL適合`，我們空白。
  - 尺寸表只差格式：正確檔 `//袖寬26`，我們 `// 袖寬26`。
- P33317 推薦尺寸：正確檔 `S-2L適合`，我們空白。

### 對款
共同差異：
- 正確檔有 `標數`，我們目前空白。
- 正確檔有 `顏色`，我們目前空白。

個別差異：
- P33296 推薦尺寸正確檔 `S-XL適合`，我們空白。
- P33315：
  - 對款品名正確檔是 `圓領素面短袖T恤上衣+針織細肩背心`，不是主表完整兩件式名稱。
  - 推薦尺寸正確檔 `S-XL適合`，我們空白。
- P33317 推薦尺寸正確檔 `S-2L適合`，我們空白。

### 姐
- 此分頁沒有流水號，改用商品名稱比對我們 10 筆。
- P33315 因正確檔品名與我們不同，名稱比對未直接對上。
- 其餘對上的商品差異主要是：
  - 正確檔有 `標數`
  - 正確檔有 `顏色`
  - P33296 / P33317 正確檔有推薦尺寸，我們空白。

### RAY
- RAY 分頁成本與我們目前抓的檔案成本一致，沒有像主表被改成 500。
- 差異主要是：
  - 正確檔有 `標數`
  - 正確檔有 `顏色`
  - P33296 / P33315 / P33317 正確檔有推薦尺寸，我們空白。
  - P33315 品名文字不同，正確為 `圓領素面短袖T恤上衣+針織細肩帶背心兩件式套裝`。

## 需要更新或待確認的規則
1. 主表成本是否固定/人工改成 `500`？RAY 分頁成本仍使用檔案成本，需確認主表與 RAY 成本邏輯是否不同。
2. `標數` 先前說空白，但正確檔有標數；需確認正式自動化要不要產生/抓取標數。
3. 顏色正確檔有值，需確認來源是否從 `流水號` 分頁 H 欄或其他人工檔案來。
4. P33296、P33315、P33317 的推薦尺寸正確檔有值，但尺寸表同列 `適合尺寸` 空白，代表可能另有補值規則或人工補。
5. `0414` 主表第 13 欄備註正確檔有值，需確認此欄來源與是否要產出。
6. 尺寸表文字規則需修正：
   - P33115 下身欄位對應錯誤。
   - P33282 肩帶欄位輸出名稱需改為 `肩帶`，不要輸出 `肩寬肩帶(17-29)`。
7. P33315 在不同分頁的品名可能不同：主表/RAY 用完整兩件式名稱；對款可能用短名稱，需確認規則。


## 標數與顏色修正確認
- 使用者確認：`標數` 仍然要空白；正確檔內的標數先不作為自動化輸出來源。
- `顏色` 不能再直接忽略，需要回頭看來源與補齊規則。
- 後續顏色要優先比對正確檔顏色、`流水號` 分頁 H 欄、以及 `本次要匯入的流水號` / `顏色尺寸請用這一個` 的產生結果。
